<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data noupdate="1">

    <record id="intent_class_list" model="ai.intent.schema">
        <field name="intent_type">class_list</field>
        <field name="name">Class List</field>
        <field name="description">List classes/sessions for a given date</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "date": {"type": "string", "format": "date", "description": "Date to list classes for (YYYY-MM-DD)"}
    }
}</field>
        <field name="example_phrases">What classes are on today?
List all classes for Friday
Show me Monday's classes
What sessions do we have this week?</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_schedule_today" model="ai.intent.schema" forcecreate="0">
        <field name="intent_type">schedule_today</field>
        <field name="name">Today's / Date Schedule</field>
        <field name="description">Get the class schedule for today or a specific date (tomorrow, yesterday, or YYYY-MM-DD)</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "date": {
            "type": "string",
            "description": "Date to get schedule for. Use 'today' (default), 'tomorrow', 'yesterday', or ISO date YYYY-MM-DD"
        }
    }
}</field>
        <field name="example_phrases">What's the schedule today?
Show today's classes
What classes are happening now?
What's on the schedule?
What do we have today?
Show tomorrow's schedule
What's on tomorrow?
Classes for tomorrow
Schedule for tomorrow
What classes are tomorrow?
What's the schedule for Monday?
Show schedule for 2026-04-20
Classes this week</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_history" model="ai.intent.schema">
        <field name="intent_type">attendance_history</field>
        <field name="name">Attendance History</field>
        <field name="description">Get attendance history for a member</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "limit": {"type": "integer", "description": "Max records to return", "default": 10}
    }
}</field>
        <field name="example_phrases">Show John's attendance history
How often has Sarah been coming in?
When did Test8 last attend?
Show me Mike's recent classes
What's Jane's attendance like?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_checkin" model="ai.intent.schema">
        <field name="intent_type">attendance_checkin</field>
        <field name="name">Check In Member</field>
        <field name="description">Check in a member for attendance</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to check in"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "session_id": {"type": "integer", "description": "Optional session ID"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Check in John
Mark Sarah as present
Test8 just arrived
Sign in Mike for class
Check in Jane Doe</field>
        <field name="confirmation_template">Check in {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_checkout" model="ai.intent.schema">
        <field name="intent_type">attendance_checkout</field>
        <field name="name">Check Out Member</field>
        <field name="description">Check out a member from attendance</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to check out"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Check out John
Mark Sarah as done for today
Test8 is leaving
Sign out Mike
Jane is heading home</field>
        <field name="confirmation_template">Check out {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_log_create" model="ai.intent.schema">
        <field name="intent_type">attendance_log_create</field>
        <field name="name">Create Attendance Entry</field>
        <field name="description">Manually create an attendance log entry</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "session_id": {"type": "integer", "description": "ID of the class session"},
        "checkin_datetime": {"type": "string", "format": "date-time", "description": "Check-in time (defaults to now)"},
        "note": {"type": "string", "description": "Optional note"}
    },
    "required": ["member_name", "session_id"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Log attendance for John at 5pm
Create an attendance entry for Sarah
Record Test8's check-in at 3:30pm</field>
        <field name="confirmation_template">Create attendance entry for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_checkin_required_today" model="ai.intent.schema">
        <field name="intent_type">checkin_required_today</field>
        <field name="name">Check-In Required Today</field>
        <field name="description">List members enrolled in today's sessions who have not yet checked in</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "session_id": {"type": "integer", "description": "Filter by a specific session ID (optional)"}
    }
}</field>
        <field name="example_phrases">Who hasn't checked in today?
Show me members that still need to check in
Which students are missing from today's class?
Who is enrolled but not here yet?
List members who need to check in</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_session_no_shows" model="ai.intent.schema">
        <field name="intent_type">session_no_shows</field>
        <field name="name">Session No-Shows</field>
        <field name="description">List members who were enrolled in a past session but did not check in (absent)</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "session_id": {"type": "integer", "description": "ID of the session to check"},
        "session_name": {"type": "string", "description": "Name of the session"}
    }
}</field>
        <field name="example_phrases">Who didn't show up to yesterday's class?
List the no-shows for last Tuesday's session
Which enrolled students were absent?
Who missed the 6pm class?
No-show list for today's session</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_rate" model="ai.intent.schema">
        <field name="intent_type">attendance_rate</field>
        <field name="name">Attendance Rate</field>
        <field name="description">Get a member's attendance statistics: total visits, visits this month, and attendance rate</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "period": {"type": "string", "description": "Time window: 'month' (default), 'quarter', or 'year'"}
    }
}</field>
        <field name="example_phrases">What's John's attendance rate?
How often does Sarah actually come in?
Show me Mike's attendance stats
What percentage of classes has Jane attended?
How many times has Test8 come this month?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_new_members" model="ai.intent.schema">
        <field name="intent_type">new_members</field>
        <field name="name">New Members</field>
        <field name="description">Count and list members who joined within a period (this month by default)</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "period": {"type": "string", "enum": ["week", "month", "year"], "description": "Time window (default: month)"}
    }
}</field>
        <field name="example_phrases">How many new members this month?
Who joined recently?
Show me new signups this month
How many people signed up this week?
New members this year</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    <record id="intent_belt_stuck" model="ai.intent.schema">
        <field name="intent_type">belt_stuck</field>
        <field name="name">Time at Current Belt</field>
        <field name="description">How long a member has been at their current belt, or list members overdue for promotion</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member (optional)"},
        "member_id": {"type": "integer", "description": "ID of the member (optional)"},
        "min_days": {"type": "integer", "description": "Minimum days at belt for the roster view (default: 180)"}
    }
}</field>
        <field name="example_phrases">How long has Alex been at his belt?
Who's overdue for a promotion?
Which members have been stuck at their belt the longest?
How long since Sarah was promoted?
Who's been at the same belt for over 6 months?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    <record id="intent_peak_hours" model="ai.intent.schema">
        <field name="intent_type">peak_hours</field>
        <field name="name">Peak Check-in Times</field>
        <field name="description">Busiest check-in hours and days based on attendance logs</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "period": {"type": "string", "enum": ["month", "90days", "year"], "description": "Look-back window (default: 90days)"}
    }
}</field>
        <field name="example_phrases">What are our busiest hours?
When is the gym most crowded?
What time do most people check in?
Peak attendance times
Which days are busiest?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    
    <record id="intent_today_checkins" model="ai.intent.schema">
        <field name="intent_type">today_checkins</field>
        <field name="name">Today's Check-ins</field>
        <field name="description">Count and list members who checked in today</field>
        <field name="category">read</field>
        <field name="parameters_schema">{"type": "object", "properties": {}}</field>
        <field name="example_phrases">Who checked in today?
How many people came in today?
Today's attendance
Who's been in today?
How many check-ins so far today?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    <record id="intent_open_spots" model="ai.intent.schema">
        <field name="intent_type">open_spots</field>
        <field name="name">Classes with Open Spots</field>
        <field name="description">Upcoming class sessions that still have available capacity</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "days": {"type": "integer", "description": "How many days ahead to look (default: 7)"}
    }
}</field>
        <field name="example_phrases">Which classes have open spots?
What classes still have room this week?
Any openings in upcoming classes?
Where are there available spots?
Show classes that aren't full</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    <record id="intent_attendance_streak" model="ai.intent.schema">
        <field name="intent_type">attendance_streak</field>
        <field name="name">Attendance Streak</field>
        <field name="description">Current consecutive-day check-in streak for a member</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="example_phrases">What's Alex's attendance streak?
How many days in a row has Sarah come in?
Is John on a streak?
Current streak for Mike
How long is Emma's check-in streak?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>
    
</data>
</odoo>
